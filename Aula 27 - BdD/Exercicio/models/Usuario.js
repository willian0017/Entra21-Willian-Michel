const { DataTypes, Sequelize, Model } = require("sequelize")
const { hashSync, compareSync } = require("bcrypt")
const { sequelize } = require("../../../Aula 26 - Sequelize/teste/models/Usuario")

class Usuario extends {
    static init(sequelize){
        super.init({
            id:
        })
    }
}